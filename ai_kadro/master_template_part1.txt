<!DOCTYPE html>
<html lang='tr'>
<head>
<meta charset='UTF-8'>
<meta name='viewport' content='width=device-width, initial-scale=1.0'>
<title>KADRO — Master Persona Roster</title>
<link href='https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700&family=JetBrains+Mono&display=swap' rel='stylesheet'>
<style>
:root {
  --bg: #09090B;
  --surface: #121217;
  --surface-bright: #1C1C24;
  --primary: #6366F1;
  --primary-dim: rgba(99, 102, 241, 0.1);
  --border: rgba(255, 255, 255, 0.08);
  --text: #F4F4F5;
  --text-muted: #A1A1AA;
  --accent-red: #EF4444;
  --accent-green: #10B981;
  --accent-blue: #3B82F6;
  --accent-purple: #8B5CF6;
}

* { box-sizing: border-box; margin: 0; padding: 0; }
body { 
  background-color: var(--bg); 
  color: var(--text); 
  font-family: 'Outfit', sans-serif; 
  line-height: 1.5;
  -webkit-font-smoothing: antialiased;
}

header {
  padding: 3rem 2rem;
  border-bottom: 1px solid var(--border);
  background: radial-gradient(circle at 0% 0%, rgba(99, 102, 241, 0.05) 0%, transparent 50%);
}

.header-content {
  max-width: 1400px;
  margin: 0 auto;
}

h1 {
  font-size: 2.5rem;
  font-weight: 700;
  letter-spacing: -0.02em;
  margin-bottom: 0.5rem;
  background: linear-gradient(to right, #FFF, #A1A1AA);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

.subtitle {
  color: var(--text-muted);
  font-size: 1.1rem;
}

.controls {
  position: sticky;
  top: 0;
  z-index: 100;
  background: rgba(9, 9, 11, 0.8);
  backdrop-filter: blur(12px);
  padding: 1.5rem 2rem;
  border-bottom: 1px solid var(--border);
}

.controls-inner {
  max-width: 1400px;
  margin: 0 auto;
  display: flex;
  gap: 1rem;
  align-items: center;
  flex-wrap: wrap;
}

.search-input {
  background: var(--surface);
  border: 1px solid var(--border);
  color: var(--text);
  padding: 0.75rem 1rem;
  border-radius: 0.75rem;
  font-size: 0.9rem;
  width: 300px;
  outline: none;
  transition: border-color 0.2s;
}

.search-input:focus { border-color: var(--primary); }

.filter-group {
  display: flex;
  gap: 0.5rem;
  overflow-x: auto;
  padding-bottom: 5px;
}

.filter-btn {
  background: var(--surface);
  border: 1px solid var(--border);
  color: var(--text-muted);
  padding: 0.6rem 1.2rem;
  border-radius: 2rem;
  font-size: 0.85rem;
  font-weight: 500;
  cursor: pointer;
  transition: all 0.2s;
  white-space: nowrap;
}

.filter-btn:hover { border-color: var(--primary); color: var(--text); }
.filter-btn.active { background: var(--primary); color: white; border-color: var(--primary); }

.grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(340px, 1fr));
  gap: 1.5rem;
  padding: 2rem;
  max-width: 1400px;
  margin: 0 auto;
}

.card {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 1rem;
  overflow: hidden;
  display: flex;
  flex-direction: column;
  transition: transform 0.2s, border-color 0.2s;
}

.card:hover {
  transform: translateY(-4px);
  border-color: rgba(99, 102, 241, 0.4);
}

.card-header {
  padding: 1.5rem;
  display: flex;
  gap: 1rem;
  align-items: flex-start;
  border-bottom: 1px solid var(--border);
}

.avatar {
  width: 60px;
  height: 60px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.25rem;
  font-weight: 600;
  flex-shrink: 0;
  background: var(--surface-bright);
}

.meta { flex: 1; min-width: 0; }
.p-no { font-family: 'JetBrains Mono', monospace; font-size: 0.75rem; color: var(--primary); font-weight: 600; margin-bottom: 0.25rem; }
.name { font-size: 1.1rem; font-weight: 600; margin-bottom: 0.25rem; }
.title { font-size: 0.85rem; color: var(--text-muted); line-height: 1.3; }

.card-body {
  padding: 1.5rem;
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 1rem;
}

.section-title {
  font-size: 0.7rem;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  color: var(--text-muted);
  font-weight: 700;
  margin-bottom: 0.5rem;
}

.section-text { font-size: 0.9rem; color: #D4D4D8; }

.pills { display: flex; flex-wrap: wrap; gap: 0.4rem; }
.pill { background: var(--surface-bright); font-size: 0.75rem; padding: 0.25rem 0.6rem; border-radius: 0.5rem; color: var(--text-muted); }

.prompt-section {
  padding: 1.5rem;
  background: rgba(0,0,0,0.15);
  border-top: 1px solid var(--border);
}

.prompt-box {
  background: var(--bg);
  border: 1px solid var(--border);
  padding: 0.75rem;
  border-radius: 0.5rem;
  font-family: 'JetBrains Mono', monospace;
  font-size: 0.75rem;
  color: var(--text-muted);
  margin-top: 0.5rem;
  max-height: 100px;
  overflow-y: auto;
  position: relative;
}

.copy-btn {
  float: right;
  background: var(--surface-bright);
  border: 1px solid var(--border);
  color: var(--text);
  font-size: 0.7rem;
  padding: 0.2rem 0.6rem;
  border-radius: 0.4rem;
  cursor: pointer;
}

.copy-btn:hover { background: var(--primary); }

.category-tag {
  display: inline-block;
  font-size: 0.65rem;
  font-weight: 700;
  text-transform: uppercase;
  padding: 0.1rem 0.4rem;
  border-radius: 0.25rem;
  margin-left: 0.5rem;
}

.cat-core { background: rgba(239, 68, 68, 0.15); color: #EF4444; }
.cat-global { background: rgba(59, 130, 246, 0.15); color: #3B82F6; }
.cat-local { background: rgba(16, 185, 129, 0.15); color: #10B981; }
.cat-archetype { background: rgba(139, 92, 246, 0.15); color: #8B5CF6; }

  .filter-row {
    max-width: 1400px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
    gap: 1rem;
    padding: 0 2rem 1.5rem 2rem;
  }

  .select-filter {
    background: var(--surface);
    border: 1px solid var(--border);
    color: var(--text-muted);
    padding: 0.6rem;
    border-radius: 0.5rem;
    font-size: 0.85rem;
    outline: none;
    cursor: pointer;
  }
  .select-filter:focus { border-color: var(--primary); color: var(--text); }

  @media (max-width: 768px) {
    .search-input { width: 100%; }
    .filter-row { grid-template-columns: 1fr 1fr; }
  }
</style>
</head>
<body>

<header>
  <div class='header-content'>
    <h1>KADRO</h1>
    <p class='subtitle'>Master Persona Roster — Agents Globally</p>
  </div>
</header>

<div class='controls'>
  <div class='controls-inner'>
    <input type='text' id='search' class='search-input' placeholder='İsim, anahtar kelime...' oninput='applyFilters()'>
    <div class='filter-group'>
      <button class='filter-btn active' onclick='setCategory("all", this)'>Tümü</button>
      <button class='filter-btn' onclick='setCategory("Core", this)'>Core</button>
      <button class='filter-btn' onclick='setCategory("Global", this)'>Global</button>
      <button class='filter-btn' onclick='setCategory("Local", this)'>Local</button>
      <button class='filter-btn' onclick='setCategory("Archetype", this)'>Archetype</button>
    </div>
  </div>
</div>

<div class='filter-row'>
  <select id='f-country' class='select-filter' onchange='applyFilters()'>
    <option value=''>Ülke: Tümü</option>
  </select>
  <select id='f-city' class='select-filter' onchange='applyFilters()'>
    <option value=''>Şehir: Tümü</option>
  </select>
  <select id='f-gender' class='select-filter' onchange='applyFilters()'>
    <option value=''>Cinsiyet: Tümü</option>
    <option value='Kadın'>Kadın</option>
    <option value='Erkek'>Erkek</option>
  </select>
  <select id='f-age' class='select-filter' onchange='applyFilters()'>
    <option value=''>Yaş: Tümü</option>
    <option value='20-30'>20-30 Yaş</option>
    <option value='30-40'>30-40 Yaş</option>
    <option value='40+'>40+ Yaş</option>
  </select>
</div>

<div class='grid' id='grid'></div>

<script>
const PERSONAS = 
